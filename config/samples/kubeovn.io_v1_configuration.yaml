apiVersion: kubeovn.io.my.domain/v1
kind: Configuration
metadata:
  labels:
    app.kubernetes.io/name: kubeovn-operator
    app.kubernetes.io/managed-by: kustomize
  name: configuration-sample
spec:
  # TODO(user): Add fields here
